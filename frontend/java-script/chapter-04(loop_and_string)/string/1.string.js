// what is string?

//A string is a sequence of characters — such as letters, numbers, symbols, or spaces — enclosed within quotes. and stored in a continuius manner in memory

// strings are immutable((you can’t directly change a character inside a string))

// we can't store  string   directly in a memory bcz computer can only store a binary number so first we have to  give  a constant  numerical number to these chracter of stirng 
// then it can be store as a binary after compilation in memory

// for this we use ASCII CODE AND UNICODE

// string is a primitve data type




// creating string:

let str = "shahid"


  //string length

  //string indices

  // template literal in js:it used to embedded expression in a string 
  `this is a template literal`


  // string interpolation:to create strings by doing substituition of placeholder 

  `stringtext${expresstion}stringtxt`






