// what is function?

// block of code which has  name for identification and used to  perform some specific task,can be invked whenever needed.

// so we can say Functions are reusable block of code

// functions are executet when they are "called" or "invoked".

// A function is defined with the function keyword, followed by the function name, followed by parentheses ( ), followed by brackets { }.

// function cosume memory when only when it is invked and released from ram as soon as it finises the job